import 'package:flutter/material.dart';

void main() => runApp(const PV());

class PV extends StatelessWidget {
  const PV({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'PV',
      home: Scaffold(
        appBar: AppBar(
  title: const Text('PV App'),
),
        ),
        body: const Center(
          child: Text('PV'),
        ),
      ),
    );
  }
}
